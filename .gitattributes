# Os arquivos de override do rAthena são implantados em host Linux
# (/opt/faithro/rathena) e DEVEM permanecer com final de linha LF,
# independentemente do core.autocrlf de quem faz checkout.
deploy/rathena-overlay/** text eol=lf
scripts/validate-progression-overrides.py text eol=lf
scripts/validate-client-assets.py text eol=lf
scripts/validate-patcher-config.py text eol=lf
scripts/generate-synthetic-patch-lab.py text eol=lf
scripts/validate-synthetic-patch-lab.py text eol=lf
scripts/audit-beam-upstream.py text eol=lf
scripts/validate-beam-build-plan.py text eol=lf
scripts/validate-beam-security-overlay.py text eol=lf

# O overlay de segurança do Beam é gerado contra o upstream (CRLF) e DEVE ser
# aplicado byte a byte com git apply. Atributos:
#   -text       impede o Git de reescrever os finais de linha (CRLF preservado);
#   diff        trata como TEXTO no diff (auditável no Git e no GitHub);
#   -whitespace não classifica os CR preservados como "trailing whitespace"
#               apenas neste arquivo (evita falso positivo em git diff --check).
# Ver client/patcher/beam-audit/overlays/.
client/patcher/beam-audit/overlays/beam-lab-security.patch -text diff -whitespace

# Fixtures sintéticas do laboratório do patcher DEVEM permanecer com LF: o
# validador anti-drift compara byte a byte com a saída determinística (LF) do
# gerador. Ver docs/18-homologacao-patch-sintetico-beam.md.
client/patcher/fixtures/synthetic/** text eol=lf

# O SHA-256 do runbook e do modelo de autorização é FIXADO na solicitação de
# autorização (D1-B12) e recomputado byte a byte pelo validador
# scripts/validate-beam-first-build-authorization-request.py. Esses arquivos
# DEVEM permanecer com LF em qualquer plataforma (incluindo checkout Windows com
# core.autocrlf=true), para que o hash fixado seja reproduzível e verificável no
# host de build. Ver docs/25-solicitacao-autorizacao-primeiro-build-beam.md.
client/patcher/beam-audit/first-build-runbook.example.json text eol=lf
client/patcher/beam-audit/first-build-authorization.example.json text eol=lf
client/patcher/beam-audit/first-build-authorization-request.example.json text eol=lf

# O pacote de decisão humana (D1-B14) persiste o SHA-256 da solicitação, do
# runbook, do modelo de autorização e do plano de build, recomputados byte a
# byte por scripts/validate-beam-first-build-human-decision.py. O plano e os
# novos artefatos de decisão DEVEM permanecer com LF para que os hashes fixados
# sejam reproduzíveis em qualquer plataforma. Ver
# docs/26-pacote-decisao-humana-primeiro-build-beam.md.
client/patcher/beam-audit/first-build-plan.example.json text eol=lf
client/patcher/beam-audit/first-build-human-decision-package.example.json text eol=lf
client/patcher/beam-audit/first-build-human-decision-record.example.json text eol=lf
